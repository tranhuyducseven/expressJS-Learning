<div class="container">
    <div class="row">
        <h1>{{post.name}}</h1>
    </div>
    <div class="row">
        <img src="{{post.image}}" class="img-fluid" alt="Responsive image" height="600">

    </div>
    <div class="row">
        <p class="h1">{{post.description}}</p>
    </div>
</div>